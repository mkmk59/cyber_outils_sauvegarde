# ===========================================
# Configuration Ghostfolio
# ===========================================
# IMPORTANT: Copiez ce fichier en .env et modifiez les valeurs
# Ne versionnez JAMAIS le fichier .env!

# PostgreSQL
POSTGRES_USER=ghostfolio
POSTGRES_PASSWORD=CHANGEZ_MOI_mot_de_passe_securise
POSTGRES_DB=ghostfolio

# Ghostfolio Secrets (generez avec: openssl rand -hex 32)
ACCESS_TOKEN_SALT=CHANGEZ_MOI_generez_avec_openssl_rand_hex_32
JWT_SECRET_KEY=CHANGEZ_MOI_generez_avec_openssl_rand_hex_32

# ===========================================
# Configuration BorgBackup
# ===========================================
# Passphrase pour le chiffrement des sauvegardes (generez avec: openssl rand -base64 32)
BORG_PASSPHRASE=CHANGEZ_MOI_passphrase_securisee

# ===========================================
# Configuration MinIO (Stockage S3)
# ===========================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGEZ_MOI_minio_password_securise

# ===========================================
# Configuration Restic (Sauvegarde config Borg)
# ===========================================
# Passphrase pour le chiffrement des sauvegardes Restic
RESTIC_PASSWORD=CHANGEZ_MOI_restic_password_securise

# ===========================================
# INSTRUCTIONS
# ===========================================
# 1. Copiez ce fichier: cp .env.example .env
# 2. Generez les secrets:
#    - openssl rand -hex 32  (pour ACCESS_TOKEN_SALT et JWT_SECRET_KEY)
#    - openssl rand -base64 32  (pour BORG_PASSPHRASE et POSTGRES_PASSWORD)
# 3. OU utilisez le script: ./generate-secrets.sh
# 4. Ne partagez JAMAIS le fichier .env!
